# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-actual-api-key-here

# Crawler Configuration
# Maximum number of articles to process per blog (default: 30)
MAX_ARTICLES_PER_URL=30

# Content Extraction
# Set to 'true' to extract full article content (slower but better posts)
# Set to 'false' to generate posts from titles only (faster)
EXTRACT_CONTENT=false

# Output Configuration
# Filename for the generated posts (CSV format)
OUTPUT_FILE=output/linkedin_posts.csv
